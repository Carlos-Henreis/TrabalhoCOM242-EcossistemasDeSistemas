GRPC: http://www.grpc.io/docs/quickstart/python.html
PROTOBUFFERS: https://developers.google.com/protocol-buffers/docs/overview

Para gerar classes do python faça:
$ protoc -I=./ --python_out=./ filename.proto

Instalação do google gRPC e do protobuf: 

1.)Primeiro atualize a versão de seu pip

$ pip3 install --upgrade pip
$ pip3 --version
pip 9.0.1

2.) Agora através do pip faça download do pacote
$ sudo pip3 install grpcio
$ sudo pip3 install grpcio-tools
$ sudo pip3 install protobuf

Obs: para gerar os protocs com o grpc
$ python -m grpc_tools.protoc -I. --python_out=. --grpc_python_out=. helloworld.proto